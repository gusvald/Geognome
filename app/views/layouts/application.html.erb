<!DOCTYPE html>
<html>
  <head>
    <title>Geognome</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  
    <ul>
      <%= link_to "Strona główna", root_path %> <br>
      -----------------------------------------------<br>
      <% if logged_in? %>
       <h2> <%= link_to current_user.login, current_user %></h2>
        <%= link_to "Wyloguj", logout_path, method: :delete %><br>
      <% else %>
        <%= link_to "Zaloguj", login_path %>
      <% end %>
    </ul>
    <%= yield %>
  </body>
</html>
